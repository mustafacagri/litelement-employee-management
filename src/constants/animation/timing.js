// Component states
export const STATES = {
	PENDING: 'pending',
	IN_PROGRESS: 'in_progress',
	COMPLETED: 'completed',
	CANCELLED: 'cancelled',
}

// Common timeout durations
export const TIMEOUT = {
	DEBOUNCE_SEARCH: 300,
	NOTIFICATION_DEFAULT: 3000,
	DROPDOWN_CLOSE: 150,
}
